Go Echo Setup
=========================================


### How To Run
```shell
docker-compose up -d --build
go run main.go runserver
```

### How to Migration
```shell
go run main.go migrate -e up
```